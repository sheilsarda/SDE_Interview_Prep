# Intersection of line and square

Given a line segment and an axis-aligned box, implement a function to determine if any part of the line segment overlaps the box

### Solution (Python)

In progress on `main.py`